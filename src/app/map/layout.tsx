import type { ReactNode } from "react";

export const metadata = { title: "Hextash — DM's Hub" };

export default function MapLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
